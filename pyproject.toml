[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint.per-file-ignores]
# The embedded vis-network template is a raw JS string that can't be reflowed.
"gradio_app.py" = ["E501"]

[tool.black]
line-length = 100
